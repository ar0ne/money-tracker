import { css } from 'lit';

export const styles = css`
  @media(min-width: 1000px) {
    sl-card {
      max-width: 70vw;
    }
  }
  main {
  }
  .center {
    display: flex;
    justify-content: center;
    align-items: center;
  }
`;