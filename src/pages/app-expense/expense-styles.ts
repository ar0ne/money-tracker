import { css } from 'lit';

export const styles = css`
  .disabled {
    color: grey;
    cursor: not-allowed;
    pointer-events: none;
    background-color: #ffffff;
  }

  .hide {
    display: none;
  }

`;